# For responsiveness 
use Media queries
no external library
make each component responsive 
# scss for styling
for styling use sass and make sure to make scss file for each component seperate
# fonts
fonts are manually downloaded roboto family try to use them
# colors
colors are assign to variable in index.scss so use them dynamically
# icons
i already set cdn of fontawesome make sure to use fontawesome icon
# fonts
font names are added to vaiable in scss so use them dynamically


